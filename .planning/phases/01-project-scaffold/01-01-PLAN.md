---
phase: 01-project-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/setupTests.ts
  - src/App.test.tsx
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run dev serves the app locally with hot module reload on port 5173"
    - "npm test executes the Vitest suite and all tests pass"
    - "Tailwind CSS utility classes render correctly (text-2xl, font-bold, bg-gray-50 are applied)"
    - "The App component renders 'Expense Splitter' heading in the browser"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite + React + Tailwind + Vitest unified config"
      contains: "tailwindcss"
    - path: "src/App.tsx"
      provides: "Root component with Tailwind-styled heading"
      contains: "Expense Splitter"
    - path: "src/App.test.tsx"
      provides: "Smoke test proving scaffold wires together"
      contains: "renders without crashing"
    - path: "src/index.css"
      provides: "Tailwind CSS entry point"
      contains: "@import"
    - path: "src/setupTests.ts"
      provides: "Test setup with jest-dom matchers and cleanup"
      contains: "jest-dom"
    - path: "package.json"
      provides: "All production and dev dependencies"
      contains: "vitest"
  key_links:
    - from: "src/main.tsx"
      to: "src/index.css"
      via: "CSS import"
      pattern: "import.*index\\.css"
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "Component import and render"
      pattern: "import App from"
    - from: "vite.config.ts"
      to: "src/setupTests.ts"
      via: "Vitest setupFiles config"
      pattern: "setupFiles.*setupTests"
    - from: "index.html"
      to: "src/main.tsx"
      via: "Script module entry"
      pattern: "src/main.tsx"
---

<objective>
Scaffold the complete Vite + React 19 + TypeScript 6 + Tailwind CSS v4 + Vitest project from scratch, producing a working dev server, passing test suite, and Tailwind-styled root component.

Purpose: Establish the testable, buildable foundation that every subsequent phase builds on. Without this, no code can be written, tested, or deployed.
Output: A fully wired project where `npm run dev` serves the app, `npm test` passes, and Tailwind classes render correctly.
</objective>

<execution_context>
@/Users/eliot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eliot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/eliot/Desktop/Courses/gsd-module-test/.planning/PROJECT.md
@/Users/eliot/Desktop/Courses/gsd-module-test/.planning/ROADMAP.md
@/Users/eliot/Desktop/Courses/gsd-module-test/.planning/STATE.md
@/Users/eliot/Desktop/Courses/gsd-module-test/.planning/phases/01-project-scaffold/01-RESEARCH.md
@/Users/eliot/Desktop/Courses/gsd-module-test/.planning/phases/01-project-scaffold/01-PATTERNS.md
</context>

## Phase Goal

**As a** developer, **I want to** have a working Vite + React + TypeScript project with Tailwind CSS, Vitest, and a deployment pipeline, **so that** every subsequent phase builds on a consistent, testable foundation.

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project and install all dependencies</name>
  <files>package.json, .gitignore, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/App.tsx, src/App.css, src/index.css, src/assets/react.svg, public/vite.svg</files>
  <read_first>
    @/Users/eliot/Desktop/Courses/gsd-module-test/.planning/phases/01-project-scaffold/01-RESEARCH.md (Standard Stack section for exact package versions; Installation commands; Architecture Patterns section for project structure)
    @/Users/eliot/Desktop/Courses/gsd-module-test/.planning/phases/01-project-scaffold/01-PATTERNS.md (all file patterns -- this IS the implementation reference for every file)
  </read_first>
  <action>
    Initialize git repo in the project root with `git init`.

    Scaffold the Vite project by running `npm create vite@latest expense-splitter -- --template react-ts` in a temporary location, then move all generated files into the project root (the project root already has CLAUDE.md, PROJECT_BRIEF.md, README.md, .planning/ that must be preserved). Do NOT overwrite existing files in the root.

    After scaffold files are in place, install production dependencies for Tailwind CSS v4: `npm install tailwindcss @tailwindcss/vite`.

    Install testing devDependencies: `npm install -D vitest @testing-library/react @testing-library/user-event @testing-library/jest-dom jsdom`.

    Verify all packages installed by confirming package.json contains: react (19.x), react-dom (19.x), tailwindcss (4.x), @tailwindcss/vite (4.x), vitest, @testing-library/react, @testing-library/jest-dom, jsdom.

    Confirm the scaffold generated these TypeScript configs: tsconfig.json (project references aggregator with no compilerOptions), tsconfig.app.json (with jsx: react-jsx, strict: true), tsconfig.node.json (includes vite.config.ts only).
  </action>
  <verify>
    <automated>cd /Users/eliot/Desktop/Courses/gsd-module-test && node -e "const p=require('./package.json'); const deps={...p.dependencies,...p.devDependencies}; const required=['react','react-dom','tailwindcss','@tailwindcss/vite','vitest','@testing-library/react','@testing-library/jest-dom','jsdom']; const missing=required.filter(r=>!deps[r]); if(missing.length){console.error('MISSING:',missing);process.exit(1)}else{console.log('All deps present')}"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains react, react-dom, tailwindcss, @tailwindcss/vite as dependencies
    - package.json contains vitest, @testing-library/react, @testing-library/jest-dom, jsdom as devDependencies
    - node_modules/ exists and is populated
    - tsconfig.json has "references" array with paths to tsconfig.app.json and tsconfig.node.json
    - tsconfig.app.json has "strict": true and "jsx": "react-jsx"
    - index.html exists at project root with div#root and script type="module" pointing to src/main.tsx
    - .gitignore exists and includes node_modules and dist
    - git repo initialized (`.git/` directory exists)
  </acceptance_criteria>
  <done>All production and dev dependencies installed. Vite scaffold files in place. Git repo initialized. TypeScript configs present with strict mode enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind, Vitest, and create clean App component with smoke test</name>
  <files>vite.config.ts, src/index.css, src/App.tsx, src/main.tsx, src/setupTests.ts, src/App.test.tsx, tsconfig.app.json, index.html</files>
  <read_first>
    @/Users/eliot/Desktop/Courses/gsd-module-test/vite.config.ts (current scaffold state -- will be modified)
    @/Users/eliot/Desktop/Courses/gsd-module-test/src/index.css (current scaffold state -- will be replaced)
    @/Users/eliot/Desktop/Courses/gsd-module-test/src/App.tsx (current scaffold state -- will be replaced)
    @/Users/eliot/Desktop/Courses/gsd-module-test/src/main.tsx (current scaffold state -- verify CSS import exists)
    @/Users/eliot/Desktop/Courses/gsd-module-test/tsconfig.app.json (current scaffold state -- needs vitest/globals type)
    @/Users/eliot/Desktop/Courses/gsd-module-test/index.html (verify title update needed)
    @/Users/eliot/Desktop/Courses/gsd-module-test/.planning/phases/01-project-scaffold/01-PATTERNS.md (authoritative patterns for every file)
  </read_first>
  <action>
    Update vite.config.ts to add the @tailwindcss/vite plugin (import tailwindcss from '@tailwindcss/vite', add tailwindcss() after react() in plugins array) and add the Vitest test block (globals: true, environment: 'jsdom', setupFiles: './src/setupTests.ts').

    Replace the entire content of src/index.css with a single line: @import "tailwindcss"; -- delete all Vite template demo styles.

    Delete src/App.css if it exists (Vite template ships it; we use Tailwind instead).

    Replace src/App.tsx with a clean component: a function App that returns a div with className="min-h-screen bg-gray-50 flex items-center justify-center" containing an h1 with className="text-2xl font-bold text-gray-900" and text content "Expense Splitter". Default export only, no React import (automatic JSX transform handles it), no other exports.

    Verify src/main.tsx imports ./index.css and renders App inside StrictMode. If the scaffold template already does this correctly, leave it. If not, update to match the pattern from 01-PATTERNS.md.

    Update index.html title element from the Vite default to "Expense Splitter".

    Update tsconfig.app.json compilerOptions.types to include both "vite/client" and "vitest/globals" (prevents TypeScript errors on jest-dom matchers like toBeInTheDocument).

    Create src/setupTests.ts with: import afterEach from vitest, import cleanup from @testing-library/react, import @testing-library/jest-dom (side-effect import), and call cleanup() in afterEach.

    Create src/App.test.tsx with a describe('App scaffold') block containing one test: 'renders without crashing' that calls render(<App />) and asserts expect(document.body).toBeInTheDocument(). No explicit vitest imports (globals: true provides describe/it/expect).

    Delete any remaining Vite scaffold demo files: src/assets/react.svg (if present), public/vite.svg (if present -- or replace with a simple favicon later).
  </action>
  <verify>
    <automated>cd /Users/eliot/Desktop/Courses/gsd-module-test && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts imports and registers both react() and tailwindcss() plugins
    - vite.config.ts contains a test block with globals: true, environment: 'jsdom', setupFiles: './src/setupTests.ts'
    - src/index.css contains exactly "@import "tailwindcss";" and no other styles
    - src/App.css does not exist
    - src/App.tsx exports a single default function component rendering h1 with text "Expense Splitter"
    - src/App.tsx contains Tailwind classes: min-h-screen, bg-gray-50, text-2xl, font-bold
    - index.html title element contains "Expense Splitter"
    - tsconfig.app.json types array contains "vitest/globals"
    - src/setupTests.ts imports @testing-library/jest-dom and calls cleanup in afterEach
    - src/App.test.tsx contains describe('App scaffold') with 'renders without crashing' test
    - `npx vitest run` exits with code 0 and reports 1 test passed
    - `npm run build` succeeds and produces a dist/ directory
  </acceptance_criteria>
  <done>Tailwind CSS v4 wired via Vite plugin. Vitest configured with jsdom and jest-dom matchers. Clean App component renders with Tailwind styling. Smoke test passes. Production build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local node_modules | Third-party packages installed from public registry |
| Local build to Vercel CDN | Built artifacts uploaded to Vercel for static hosting |
| Browser to Vercel CDN | End users fetch static assets from Vercel edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install (all packages) | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit against npm registry with authoritative source repos. Use exact versions in package-lock.json. Do not run npm install with --ignore-scripts disabled for unknown packages. |
| T-01-02 | Tampering | Vite build pipeline | accept | Vite is a widely-used build tool from vitejs org. Build output is deterministic static HTML/JS/CSS. No server-side execution. Risk is low for a client-side-only app. |
| T-01-03 | Information Disclosure | Vercel deployment secrets | mitigate | No secrets exist in Phase 1 (no API keys, no env vars with sensitive data). vercel.json contains only the SPA rewrite rule. No .env files committed. .gitignore excludes node_modules and dist. |
| T-01-04 | Tampering | vercel.json SPA rewrite | accept | The catch-all rewrite (/(.*) to /index.html) is standard for SPAs. No server-side logic exists to bypass. All rendering is client-side. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | All packages tagged [ASSUMED] in RESEARCH.md Package Legitimacy Audit (slopcheck unavailable). All packages from established organizations (Meta, Microsoft, Tailwind Labs, vitejs, vitest-dev, testing-library, jsdom). No [SLOP] or [SUS] packages. Human checkpoint for any future package additions not in the audit. |
</threat_model>

<verification>
1. `npm run dev` starts Vite dev server on port 5173 and serves the app
2. `npx vitest run` executes test suite with all tests passing
3. `npm run build` produces dist/ directory with index.html and assets
4. src/App.tsx contains Tailwind utility classes that will render when viewed in browser
</verification>

<success_criteria>
- Dev server runs with HMR active
- All Vitest tests pass (1 smoke test minimum)
- Tailwind classes are present in App component (visual verification deferred to Plan 02)
- Production build succeeds without errors
- No TypeScript errors (strict mode)
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffold/01-01-SUMMARY.md` when done
</output>
